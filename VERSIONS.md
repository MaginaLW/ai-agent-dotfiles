# Versions

This file is a lightweight troubleshooting log for local tool versions. It must not contain tokens, account identifiers, machine-specific secrets, or login state.

## Local Records

No local CLI versions have been recorded yet.

Suggested commands:

```powershell
pwsh -NoProfile -Command '$PSVersionTable.PSVersion'
git --version
claude --version
codex --version
```
