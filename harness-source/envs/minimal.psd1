@{
    SchemaVersion = 1
    Name = 'minimal'
    Description = '最小验证环境'
    Profile = 'base'

    Skills = @{
        Claude = @(
            'brainstorming'
        )
        Codex = @(
            'brainstorming'
        )
        Reasonix = @(
            'brainstorming'
        )
    }

    McpTemplates = @()
}
