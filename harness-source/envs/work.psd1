@{
    SchemaVersion = 1
    Name = 'work'
    Description = '精简日常编码环境；计划与审查沿用项目规则，专用 skills 按需选择'
    Profile = 'coding'

    Skills = @{
        Claude = @(
            'systematic-debugging'
        )
        Codex = @(
            'systematic-debugging'
        )
        Reasonix = @(
            'systematic-debugging'
        )
    }
}
